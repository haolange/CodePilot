- 所有运行时实现必须遵循 `AgentRuntime` 接口契约，并通过 `registerRuntime` 显式注册。
- 底层异构事件必须在适配器层转换为 `RuntimeRunEvent` 联合类型，禁止直接向 UI 透传原始运行时私有事件。
- 会话元数据（Session Ref）的读写必须通过 `session-store` 提供的抽象 API 进行，严禁直接操作数据库字段以实现运行时隔离。