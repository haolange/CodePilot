- **分层目录结构**：`exec-plans/`（Active/Completed）管控中大型功能生命周期；`guardrails/` 沉淀模块级不变量（如 Runtime、Provider、Schema）；`handover/` 记录系统架构与数据流；`research/` 存放可行性分析与 POC。
- **闭环工作流**：遵循 Signal → Triage → Fix → Verify → Guardrail 流程，强制要求 Smoke Ledger 登记真实凭据与验证结果。
- **接口边界**：通过 `CLAUDE.md` 定义 AI 检索规则，确保新代码符合 `design.md` 的视觉规范与 `ui-governance.md` 的四层组件架构。