- 所有构建与工具脚本均采用 ES Module (`.mjs`) 格式，并通过 `import.meta.url` 动态解析仓库根路径以确保跨环境兼容性。
- 主题配置文件遵循统一的 JSON 结构，必须包含 `id`、`label`、`order` 以及分别针对 `light` 和 `dark` 模式的完整 OKLCH 语义色板。
- 涉及系统命令执行的脚本（如签名、图标转换）均包含完善的错误捕获与降级处理逻辑，并在关键步骤输出详细的日志信息。